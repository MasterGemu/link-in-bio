name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16' # Utiliser une version de Node.js appropriée

      - name: Deploy to GitHub Pages
        env:
          GITHUB_TOKEN: ${{ secrets.MY_SECRET_TOKEN }}  # Assurez-vous que le secret est bien défini
        run: |
          # Ici, vous pouvez ajouter des commandes pour construire et déployer votre projet
          echo "Déploiement réussi avec le token."
